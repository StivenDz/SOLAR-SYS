name: Deployment

on:
  push:
    branches:
      - master

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: 
      name: production
      url: https://stivendz.github.io
      source: src/templates/
    steps:
      - name: deploy
